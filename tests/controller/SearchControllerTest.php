<?php
/**
 * This class is a part of the package controller and the package
 * is a part of the project expression.
 *
 * Integrated ICT Pvt. Ltd. Jwagal, Lalitpur, Nepal.
 * https://www.integratedict.com.np
 * https://www.semantro.com
 *
 * Created by Santa on 2023-07-29, 12:54 PM.
 */

namespace controller;

use PHPUnit\Framework\Assert;
use PHPUnit\Framework\TestCase;

class SearchControllerTest extends TestCase
{

    /** @test
     * Verify the number of variables.
     * */
    public function test_read_variables_it()
    {
        Assert::assertSame(true, true);
        //Needs to work
    }

    /** @test
     * List number of rules.
     */
    public function test_list_rules()
    {
        Assert::assertSame(true, true);
        //Needs to work
    }

    /** @test
     * Verify the compute marks.
     */
    public function test_compute_marks()
    {
        Assert::assertSame(true, true);
        //Needs to work
    }
}
